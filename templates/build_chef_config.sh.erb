#!/bin/bash
set -e

# Generate the node.json file with roles specified by the command line
{
  "run_list": [ "role[server]" ]
}

node=$(cat << EOF
{
  "run_list": [ "<%= roles.join('" "') %>" ]
}
EOF
)

echo $node > <%= CHAOS_CHEF_NODE_PATH %>